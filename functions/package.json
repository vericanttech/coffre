{
  "name": "coffre-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Coffre: OCR, redaction, Gemini extraction",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@google-cloud/vision": "^4.0.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  },
  "private": true
}
